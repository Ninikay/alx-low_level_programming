#include "main.h"

/**
 * largest_number - To return the largest of three integers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest integer
 */

int largest_number(int a, int b, int c)
{
	int largest;

	if (a >= b && a >= c)
	{
		Largest a;
	}
	else if (b >= a && b >= c)
	{
		Largest b;
	}
	else
	{
		largest = c;
	}

	return (largest);
}
