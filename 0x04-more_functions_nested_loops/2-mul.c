#include "main.h"

/**
 * mul -  A function that multiplies two integers
 * @a: Input, the first integer
 * @b: Input, the second interger to multiple with the first
 *
 * Return: Multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
